import { Col, Row } from 'react-bootstrap';
import styled from 'styled-components';

export const RowProducts = styled(Row)`
  min-height: 100%;
`;

export const ColFilter = styled(Col)`
  padding-left: 0;
`;

export const ColProducts = styled(Col)`
  padding: 0;
`;
