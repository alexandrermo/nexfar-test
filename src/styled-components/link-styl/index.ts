import { Link } from 'react-router-dom';
import styled from 'styled-components';

export const LinkStyl = styled(Link)`
  color: unset;

  &:hover {
    color: unset;
    text-decoration: none;
  }
`;
